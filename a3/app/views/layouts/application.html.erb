<!DOCTYPE html>
<html>
  <head>
    <title>invoices</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <script src="https://use.fontawesome.com/releases/v5.15.1/js/all.js" crossorigin="anonymous"></script>

    <!-- Bootstrap core JS-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>

  </head>

  <body>
    <header style="width: 1200px; margin: 0 auto;">
      <% flash.each do |message_type, message| %>
        <div class="<%= message_type %>"><%= message %></div>
      <% end %>
      <ul>
        <% if user_signed_in? %>
          <li><%= link_to "Mi cuenta", account_path %></li>
          <li><%= link_to "Facturas", invoices_index_path %></li>
           <li><%= button_to "Salir", logout_path, method: :delete, class:"btn btn-danger" %></li>


        <% else %>
           <li><%= link_to "Entrar", login_path %></li>
           <li><%= link_to "Registro", sign_up_path %></li>
           <li><%= link_to "Olvido su contrasena", new_password_path %></li>
           <li><%= link_to "Cofirmar", new_confirmation_path %></li>
        <% end %>
      </ul>
      <hr/>
    </header>
    <main style="width: 1200px; margin: 0 auto;"> 
      <%= yield %>
    </main>
  </body>
</html>
